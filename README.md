# Dev & AI Toolkit Landing Page

一个简约 Apple 风格的开发者与 AI 工具导航页，静态站点可直接部署到 Vercel。

## 本地预览

```bash
python3 -m http.server 4173
```

然后访问 <http://localhost:4173>

## 部署到 Vercel

1. 将仓库推送到 GitHub。
2. 在 Vercel 中导入该仓库。
3. Framework Preset 选择 `Other`（或保留自动检测）。
4. 直接 Deploy 即可（无需构建命令）。
